# Aether Blockchain — Claude Code Task Queue

## Instructions

Replace the tasks below with your actual work items. The runner script (`run-claude.sh`)
feeds this file to Claude Code as the prompt.

## Current Tasks

1. Run `cargo test --all-features --workspace` and report any failures
2. Run `cargo clippy --all-targets --all-features -- -D warnings` and fix any new warnings
3. If all tests pass, update progress.md with current status
