[package]
name = "aether-keytool"
version.workspace = true
edition.workspace = true
description = "Key generation, management, and inspection tool for Aether validator and user keys"
categories = ["command-line-utilities", "cryptography"]
keywords = ["aether", "keytool", "keys", "validator"]

[dependencies]
anyhow.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
hex = "0.4"

aether-crypto-primitives = { path = "../../crypto/primitives" }

[dev-dependencies]
tempfile = "3"
proptest = "1"
